server:
  port: 8080

application:
  security:
    jwt:
      secret-key: APPSPORT
      expiration: 7600
      refresh-token:
        expiration: 7000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sportsdb
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: create-drop
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
#    show-sql: false